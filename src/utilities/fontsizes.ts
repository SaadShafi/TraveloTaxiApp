import { width } from '.';
export const fontSizes: Record<string, number> = {
  xsm: width * 0.03,
  sm: width * 0.035,
  sm2: width * 0.039,
  md: width * 0.045,
  lg: width * 0.05,
  lg2: width * 0.055,
  xl: width * 0.085,
  xxl: width * 0.1,
};
