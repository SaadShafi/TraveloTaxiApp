import { ImageSourcePropType } from 'react-native';

const images: { [key: string]: ImageSourcePropType } = {
    background: require('../Images/background.png'),
    test: require('../Images/Mask.png'),
};

export default images;
